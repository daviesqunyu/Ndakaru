# Copy this file to .env and fill in values.
# The site builds fine without them (studio runs in preview mode).

# Supabase — get from Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Media Studio passcode gate (change from default)
VITE_STUDIO_PIN=ndakaru2026
